# 电路原理每日复习+健身日程网站

按用户提供的每日复习+健身日程表制作：
- 固定作息模板
- 每日 6—7.5 小时复习
- 健身减肥时间
- 午睡和晚上休闲
- 电路原理邱关源1—8章
- 蜂考B站课时跳转
- 默认跳过P2讲义领取
- 云同步、打卡、导出、打印、一键清空

Render 环境变量：
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY

Supabase 运行 supabase.sql。


## 本版显示修正
网课卡片不再显示“B站P几”，而是显示具体课时名称：
- 课时01 电路模型和电路定律
- 课时02 电阻电路的等效变换
- ...
- 课时11 相量法

跳转逻辑仍然自动跳过 P2 讲义领取。


## 本版新增

- 健身加入每日打卡项：`健身+爬坡有氧完成`
- 网课卡片不再写“参考时长”
- 部署到 Render 后，后端会通过 B站接口读取每个分P真实时长
- P2 讲义领取会自动跳过：课时01→P1，课时02→P3，课时11→P12

如果本地直接打开 HTML，由于没有后端 `/api/bili/pages`，真实时长会显示“正在获取真实时长”。
部署到 Render 后才会自动读取真实时间。


## 本版修复
- 顶部“剩余网课”改为部署后按 B站真实时长动态计算
- 每日卡片“重要度”旁边的“网课时间”改为对应课时真实时长
- 不再显示旧估算值，例如 630分、55分钟

## 长期使用与部署建议

### 本地启动
```bash
npm install
npm start
```
启动后访问 `http://localhost:3000`。

### GitHub Pages / Vercel 静态部署
- 可以直接把 `public/index.html` 作为静态页面部署。
- 静态部署下，打卡、备注、补充任务、学习/健身分类统计会保存在浏览器 `localStorage` 中，刷新页面仍会保留。
- 静态部署没有 Node 后端，因此云同步接口 `/api/sync/*` 和 B 站真实时长接口 `/api/bili/pages` 不可用；页面会继续使用本地数据和缓存。

### Render 部署
- 使用 `npm start` 启动 Node/Express 服务。
- 如需云同步，请配置环境变量：
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`
- Render 部署后可使用云同步和后端 B 站分 P 时长读取。
