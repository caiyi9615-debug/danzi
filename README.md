# 5.31—7.3 期末复习打卡网站

按用户提供的 2026-05-31 到 2026-07-03 复习计划制作：
- 固定作息模板
- 5.31—6.10 邱关源《电路原理》第1—9章复习保持不动
- 6.11—7.3 按“每天最多两门主科”安排高数下、线代、大学物理和考前冲刺
- 每日任务卡片：上午、下午、晚上、健身+爬坡有氧、今日复盘
- 健身安排：15:20—17:20 健身 + 路程，三分化训练，结束后坡度12、速度4、约30分钟爬坡有氧
- 蜂考 B 站课时跳转，自动跳过讲义领取分 P
- 后端 `/api/bili/pages` 读取电路 B 站分 P 真实时长
- 新增计划中大物按教材章节，高数下/线代按已录入模块目录，高数上仅可选补漏
- 云同步、打卡、备注、导出、打印/导出PDF、一键清空、本地 `localStorage` 保存

Render 环境变量：

```text
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
```

Supabase 运行 `supabase.sql`。

## 网课课时显示

页面网课卡片不显示分 P 编号，只显示课时名称：
- 课时01 电路模型和电路定律
- 课时02 电阻电路的等效变换
- 课时03 电阻电路的一般分析（一）
- 课时04 电阻电路的一般分析（二）
- 课时05 电路定理（一）
- 课时06 电路定理（二）
- 课时07 含有理想运放的电阻电路
- 课时08 储能元件
- 课时09 一阶电路与二阶电路的时域分析（一）
- 课时10 一阶电路与二阶电路的时域分析（二）
- 课时11 向量法
- 课时12 正弦稳态电路的分析（一）
- 课时13 正弦稳态电路的分析（二）

跳转逻辑会自动跳过讲义领取：课时01 对应第一个正式视频，课时02 对应第三个分 P，后续课时顺延到课时13。

## 真实时长

- 部署到 Render 后，后端会通过 B 站接口读取每个分 P 真实时长。
- 页面每个课时卡片显示“真实时长：xx分xx秒”。
- 顶部“剩余网课”和每日卡片“网课：xx分xx秒”会根据真实时长动态计算。
- 如果本地直接打开 HTML 或接口暂时失败，真实时长会显示“读取中”，页面仍可继续打卡和保存本地数据。

## 本地启动

```bash
npm install
npm start
```

启动后访问 `http://localhost:3000`。

## Render 部署

- 使用 `npm start` 启动 Node/Express 服务。
- 如需云同步，请配置环境变量：
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`
- Render 部署后可使用云同步和后端 B 站分 P 时长读取。

## 静态部署说明

可以直接把 `public/index.html` 作为静态页面部署。静态部署下，打卡、备注、补充任务、学习/健身分类统计会保存在浏览器 `localStorage` 中；云同步接口 `/api/sync/*` 和 B 站真实时长接口 `/api/bili/pages` 不可用，页面会继续使用本地数据和缓存。
